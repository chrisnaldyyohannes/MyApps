<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="StandartButton">
        <item name="android:background">@drawable/standart_button_rounded</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/standart_button_textSize</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:layout_margin">@dimen/standart_button_margin</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
    </style>
    
    <style name="StandartButtonGrey" parent="StandartButton">
        <item name="android:background">@drawable/standart_rounded_button_grey</item>
    </style>

    <style name="SendButton">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/standart_button_textSize</item>
        <item name="android:background">@drawable/send_button</item>
    </style>

    <style name="ChatBubble">
        <item name="android:textSize">@dimen/chat_text_size</item>
        <item name="android:minHeight">@dimen/chat_standard_size</item>
        <item name="android:maxWidth">@dimen/chat_max_size</item>
    </style>
</resources>
